如果loadstring(game:http get " https://pastebin . com/raw/skq 4 yd 4 e ")()~ = '这是防窃的Hasl脚本UI '那么
while wait() do
打印('你个傻逼还想盗用Hasl的UI’)
结束
结束
重复
等待()
直到游戏:已加载()
本地论坛= {}
本地主题= {
RedTheme = {
MainColor = Color3.fromRGB(36，36，36)，
MainHeaderColor = color 3 . from RGB(141，56，56)，
MainHeaderColorImage = color 3 . from RGB(130，51，51)，
TainerHdlderColor = color 3 . from RGB(36，36，36)，
new pagecolor = color 3 . from RGB(193，76，76)，
section main color = color 3 . from RGB(32，32，32)，
Color = Color3.fromRGB(141，56，56)，
Image = Color3.fromRGB(130，51，51)，
mouse enter color = color 3 . from RGB(121，48，48)，
textbox color = color 3 . from RGB(109，43，43)，
FontColor = Color3.fromRGB(255，101，101)，
SliderButtonColor = color 3 . from RGB(141，56，56)，
SliderColor = Color3.fromRGB(109，43，43)，
DropOpenColor = color 3 . from RGB(141，56，56)，
drop image color = color 3 . from RGB(235，93，93)，
drop frame color = color 3 . from RGB(84，33，33)，
SampleColor = Color3.fromRGB(36，36，36)，
        SeperatorColor = Color3.fromRGB(141, 56, 56)
    },
    DarkTheme = {
        MainColor = Color3.fromRGB(36, 36, 36),
        MainHeaderColor = Color3.fromRGB(30, 30, 29),
        MainHeaderColorImage = Color3.fromRGB(27, 27, 27),
        TainerHdlderColor = Color3.fromRGB(36, 36, 36),
        NewPageColor = Color3.fromRGB(95, 95, 95),
        SectionMainColor = Color3.fromRGB(32, 32, 32),
        Color = Color3.fromRGB(36, 36, 36),
        Image = Color3.fromRGB(30, 30, 30),
        MouseEnterColor = Color3.fromRGB(45, 45, 45),
        TextBoxColor = Color3.fromRGB(30, 30, 30),
        FontColor = Color3.fromRGB(255, 255, 255),
        SliderButtonColor = Color3.fromRGB(26, 26, 26),
        SliderColor = Color3.fromRGB(255, 255, 255),
        DropOpenColor = Color3.fromRGB(36, 36, 36),
        DropImageColor = Color3.fromRGB(255, 255, 255),
        DropFrameColor = Color3.fromRGB(26, 26, 26),
        SampleColor = Color3.fromRGB(36, 36, 36),
        SeperatorColor = Color3.fromRGB(95, 95, 95)
    }
}
local minimized = false;

local tween = game:GetService("TweenService")
local tweeninfo = TweenInfo.new
local input = game:GetService("UserInputService")
local run = game:GetService("RunService")

function Forums:DraggingEnabled(frame, parent)
    parent = parent or frame

    -- stolen from wally or kiriot, kek
    local dragging = false
    local dragInput, mousePos, framePos

    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            mousePos = input.Position
            framePos = parent.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            dragInput = input
        end
    end)

    input.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - mousePos
            game.TweenService:Create(parent, TweenInfo.new(0.08, Enum.EasingStyle.Linear, Enum.EasingDirection.In), {
                Position = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale,
                    framePos.Y.Offset + delta.Y)
            }):Play()
            --parent.Position  = UDim2.new(framePos.X.Scale, framePos.X.Offset + delta.X, framePos.Y.Scale, framePos.Y.Offset + delta.Y)
        end
    end)
end

function UIMinimize()
    if minimized then
        game.TweenService:Create(game.CoreGui[getgenv().libName].shadow.Main,
            TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
                Size = UDim2.new(0, 486, 0, 283)
            }):Play()
        wait()
        game.TweenService:Create(game.CoreGui[getgenv().libName].shadow,
            TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
                ImageTransparency = 0
            }):Play()
        minimized = false
    else
        game.TweenService:Create(game.CoreGui[getgenv().libName].shadow,
            TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
                ImageTransparency = 1.000
            }):Play()
        wait()
        game.TweenService:Create(game.CoreGui[getgenv().libName].shadow.Main,
            TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
                Size = UDim2.new(0, 0, 0, 0)
            }):Play()
        minimized = true
    end
end

function Forums.new(Themes, close_callback)
    if not Themes then
        Themes = Theme.RedTheme
    end
    if Themes == "RedTheme" then
        Themes = Theme.RedTheme
    elseif Themes == "DarkTheme" then
        Themes = Theme.DarkTheme
    end
    for i, v in pairs(game.CoreGui:GetChildren()) do
        if v.Name == 'Hasl' then
            v:Destroy()
        end
    end
    Themes = Themes or {}
    getgenv().libName = 'Hasl';

    local _81asf91z9asf1 = Instance.new("ScreenGui")
    local shadow = Instance.new("ImageLabel")
    local Main = Instance.new("Frame")
    local Open = Instance.new("ImageButton")
    local OpenUICorner = Instance.new("UICorner")
    local header = Instance.new("Frame")
    local title = Instance.new("TextLabel")
    local triangle1 = Instance.new("ImageLabel")
    local triangle = Instance.new("ImageLabel")
    local close = Instance.new("ImageButton")
    local containerHolder = Instance.new("Frame")
    local pages = Instance.new("Folder")
    local newPage = Instance.new("ScrollingFrame")
    local sectionList = Instance.new("UIListLayout")
    local UIListLayout = Instance.new("UIListLayout")
    local UIListLayout_2 = Instance.new("UIListLayout")

    Forums:DraggingEnabled(header, shadow)

    UIListLayout.Parent = Main
    UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder

    _81asf91z9asf1.Name = 'Hasl'
    _81asf91z9asf1.Parent = game.CoreGui
    _81asf91z9asf1.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    _81asf91z9asf1.Changed:Connect(function(property)
        print(property)
        if property == 'Name' then
            _81asf91z9asf1:Destroy()
        end
    end)

    Open.Parent = _81asf91z9asf1
    Open.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    Open.BackgroundTransparency = 1.000
    Open.BorderColor3 = Color3.fromRGB(0, 0, 0)
    Open.BorderSizePixel = 0
    Open.Position = UDim2.new(0.0803011283, 0, 0.472511142, 0)
    Open.Size = UDim2.new(0, 50, 0, 50)
    Open.ZIndex = 1
    Open.Image = "rbxassetid://14494607687"
    Open.ImageTransparency = 0.500
    Open.Active = true
    Open.Drag
